sudo: required

language: python
python:
  - "3.6"

services:
  - docker

install:
  - docker build -q -t htmap-test --file docker/Dockerfile .

script:
  - docker run htmap-test bash -e -c "pytest -n 10 --cov ; codecov -t 492519e2-1bcf-4e8a-8a3e-e28be5d9de8d"
